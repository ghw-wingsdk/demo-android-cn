<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wa.sdk.cn.demo">

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name="com.wa.sdk.WAApplication"
        android:theme="@style/AppTheme">

        <!-- 小米 SDK START -->
        <!-- 小米 app id -->
        <meta-data
            android:name="com.wa.sdk.XIAOMI_APP_ID"
            android:value="@string/xiaomi_app_id" />

        <!-- 小米 app key -->
        <meta-data
            android:name="com.wa.sdk.XIAOMI_APP_KEY"
            android:value="@string/xiaomi_app_key" />
        <!-- 小米 SDK END -->
    </application>

</manifest>